<nav class="navbar navbar-dark bg-blue fixed-top navbar-transparent " color-on-scroll="400">
  <a class="navbar-brand-s text_nav_new" href="/">Nawas</a>
  <% if user_signed_in? %>
  <a class="navbar-brand-s text_nav_new" href="/"> welcome <%= current_user.email %> </a>
  <% end %>

  <button class="navbar-toggler text_nav_new" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navigation-index" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>

  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <%= link_to 'Home', root_path, class: "nav-link text_nav #{yield(:blog_active)}" %>
      </li>
      <%   if user_signed_in? && current_user.admin? %>
      <li class="nav-item">
        <%= link_to 'store',  books_path, class: "nav-link text_nav #{yield(:blog_active)}" %>
      </li>
          <% end %>

      <% if !user_signed_in? %>

      <li class="nav-item">
        <%= link_to 'Log In',   user_session_path, class: "nav-link text_nav #{yield(:blog_active)}" %>
      </li>
      <li class="nav-item">
        <%= link_to 'Register',  new_user_registration_path, class: "nav-link text_nav #{yield(:blog_active)}" %>
      </li>
      <% end %>

      <% if user_signed_in? %>
      <li class="nav-item">
        <%= link_to 'Log out',  destroy_user_session_path, class: "nav-link text_nav #{yield(:blog_active)}",  method: :delete %>
        <!-- Users -->
      </li>
      <% end %>

    </ul>
    <form class="form-inline my-2 my-lg-0">
      <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
      <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
    </form>
  </div>
</nav>
